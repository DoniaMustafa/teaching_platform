import '../../../../core/export/export.dart';

class BuildSecondWidget extends StatelessWidget {
  const BuildSecondWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
