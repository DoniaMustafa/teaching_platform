import 'package:teaching/core/export/export.dart';

class ResultExamScreen extends StatelessWidget {
  const ResultExamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return CustomBackground(
        child: Container(
      color: AppColors.red,
    ));
  }
}
