enum RoleType { Student, Parent, Teacher, ProfessionalCourses, School, Assistant, ProfessionalLecturer }
enum FileTypes { REMOVE, CAMERA, GALLERY }

enum ListStatus {
  listLoaded,
  listError,
  listLoading,
  initial,
}
enum DialogAnimation { DEFAULT, ROTATE, SLIDE_TOP_BOTTOM, SLIDE_BOTTOM_TOP, SLIDE_LEFT_RIGHT, SLIDE_RIGHT_LEFT, SCALE }

enum AlertTypes {
  loading,
  error,
  success,
  warning,
}

enum DialogTimingTypes { long, short }

enum AnimationsType { fade, scale, rotate, slide, slideBottomTop }

